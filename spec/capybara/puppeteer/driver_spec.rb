RSpec.describe Capybara::Puppeteer::Driver do
  it "has a version number" do
    expect(Capybara::Puppeteer::Driver::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
