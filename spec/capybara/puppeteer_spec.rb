# frozen_string_literal: true

RSpec.describe Capybara::Puppeteer do
  it 'has a version number' do
    expect(Capybara::Puppeteer::VERSION).not_to be nil
  end

  xit 'does something useful' do
    expect(false).to eq(true)
  end
end
